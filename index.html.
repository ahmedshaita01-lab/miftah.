<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Miftāḥ — Islamisches Schlüsselwörterbuch</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Amiri:wght@400;700&family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #F7F8FB; --card: #FFFFFF; --card-soft: #FAFBFD;
  --ink: #0F172A; --ink-soft: #475569; --ink-mute: #94A3B8;
  --border: #E2E8F0; --border-soft: #EEF2F7;
  --accent: #4F46E5; --accent-soft: #EEF0FF; --accent-hover: #4338CA;
  --pflicht-bg: #4F46E5; --pflicht-fg: #FFFFFF;
  --option-bg: #FEF3C7; --option-fg: #92400E;
  --rating-good-bg: #DCFCE7; --rating-good-fg: #166534;
  --rating-mid-bg: #FEF3C7; --rating-mid-fg: #854D0E;
  --rating-bad-bg: #FEE2E2; --rating-bad-fg: #991B1B;
  --shadow-sm: 0 1px 2px rgba(15, 23, 42, 0.04), 0 1px 1px rgba(15, 23, 42, 0.03);
  --shadow-md: 0 4px 6px -1px rgba(15, 23, 42, 0.06), 0 2px 4px -2px rgba(15, 23, 42, 0.04);
  --shadow-lg: 0 10px 15px -3px rgba(15, 23, 42, 0.08), 0 4px 6px -4px rgba(15, 23, 42, 0.05);
  --radius-sm: 8px; --radius: 14px; --radius-lg: 20px;
  --sans: 'Inter', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
  --arab: 'Amiri', 'Traditional Arabic', serif;
}
* { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }
body { background: var(--bg); color: var(--ink); font-family: var(--sans); font-size: 15px; line-height: 1.55; -webkit-font-smoothing: antialiased; letter-spacing: -0.005em; }

.topbar { position: sticky; top: 0; z-index: 50; background: rgba(247, 248, 251, 0.85); backdrop-filter: saturate(180%) blur(12px); -webkit-backdrop-filter: saturate(180%) blur(12px); border-bottom: 1px solid var(--border-soft); }
.topbar-inner { max-width: 1180px; margin: 0 auto; padding: 14px 24px; display: flex; align-items: center; justify-content: space-between; gap: 16px; }
@media (max-width: 700px) { .topbar-inner { flex-wrap: wrap; padding: 10px 14px; gap: 10px; } }
.brand { display: flex; align-items: center; gap: 10px; font-weight: 700; font-size: 17px; letter-spacing: -0.02em; color: var(--ink); cursor: pointer; border: none; background: transparent; font-family: inherit; }
.brand-mark { width: 32px; height: 32px; background: linear-gradient(135deg, #6366F1 0%, #8B5CF6 100%); border-radius: 9px; display: grid; place-items: center; color: white; font-family: var(--arab); font-size: 18px; line-height: 1; box-shadow: var(--shadow-md); }
.search-bar { flex: 1; max-width: 380px; position: relative; }
.search-bar input { width: 100%; padding: 9px 14px 9px 38px; background: var(--card); border: 1px solid var(--border); border-radius: 10px; font-family: inherit; font-size: 14px; color: var(--ink); outline: none; transition: all 0.15s; }
.search-bar input:focus { border-color: var(--accent); box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.1); }
.search-bar svg { position: absolute; left: 12px; top: 50%; transform: translateY(-50%); width: 16px; height: 16px; color: var(--ink-mute); }
.topbar-nav { display: flex; gap: 2px; background: var(--border-soft); padding: 3px; border-radius: 10px; }
.topbar-nav button { background: transparent; border: none; padding: 7px 12px; border-radius: 7px; font-family: inherit; font-size: 13px; font-weight: 600; color: var(--ink-soft); cursor: pointer; transition: all 0.15s; }
.topbar-nav button:hover { color: var(--ink); }
.topbar-nav button.active { background: var(--card); color: var(--ink); box-shadow: var(--shadow-sm); }

.search-dropdown { position: absolute; top: calc(100% + 6px); left: 0; right: 0; background: var(--card); border: 1px solid var(--border); border-radius: 12px; box-shadow: var(--shadow-lg); padding: 6px; display: none; max-height: 320px; overflow-y: auto; z-index: 60; }
.search-dropdown.open { display: block; }
.search-dropdown-empty { padding: 16px; text-align: center; font-size: 13px; color: var(--ink-mute); }
.search-item { display: flex; align-items: center; gap: 12px; padding: 9px 12px; border-radius: 8px; cursor: pointer; }
.search-item:hover { background: var(--accent-soft); }
.search-item .ar { font-family: var(--arab); font-size: 22px; color: var(--ink); line-height: 1; min-width: 32px; text-align: center; }
.search-item-meta { display: flex; flex-direction: column; gap: 2px; }
.search-item-name { font-weight: 600; font-style: italic; font-size: 14px; }
.search-item-trans { font-size: 12px; color: var(--ink-mute); }

.shell { max-width: 1180px; margin: 0 auto; padding: 28px 24px 80px; }
@media (max-width: 700px) { .shell { padding: 16px 14px 60px; } }
.with-aside { display: grid; gap: 24px; }
@media (min-width: 1000px) { .with-aside { grid-template-columns: 1fr 320px; gap: 28px; } }

.notice-banner { background: linear-gradient(135deg, #FEF3C7 0%, #FDE68A 100%); border-radius: var(--radius); padding: 14px 18px; font-size: 13px; color: var(--option-fg); margin-bottom: 16px; display: flex; align-items: flex-start; gap: 10px; line-height: 1.5; }
.notice-banner strong { font-weight: 700; }

.view-header { margin-bottom: 24px; }
.view-title { font-size: 28px; font-weight: 700; letter-spacing: -0.02em; margin-bottom: 6px; }
.view-subtitle { font-size: 15px; color: var(--ink-soft); }
.view { display: none; }
.view.active { display: block; }

.breadcrumb { display: flex; align-items: center; gap: 8px; font-size: 13px; color: var(--ink-mute); margin-bottom: 12px; }
.breadcrumb button { color: var(--ink-soft); background: none; border: none; cursor: pointer; font-family: inherit; font-size: inherit; padding: 0; }
.breadcrumb button:hover { color: var(--accent); }
.breadcrumb .sep { opacity: 0.5; }

.hero { background: var(--card); border-radius: var(--radius-lg); padding: 36px 32px; box-shadow: var(--shadow-md); display: grid; gap: 24px; position: relative; overflow: hidden; }
.hero::before { content: ''; position: absolute; top: -120px; right: -120px; width: 300px; height: 300px; background: radial-gradient(circle, rgba(79, 70, 229, 0.08) 0%, transparent 70%); pointer-events: none; }
.hero-main { display: grid; grid-template-columns: 1fr auto; gap: 24px; align-items: end; position: relative; }
@media (max-width: 600px) { .hero { padding: 28px 22px; } .hero-main { grid-template-columns: 1fr; } }
.hero-text h1 { font-size: 48px; font-weight: 700; letter-spacing: -0.03em; line-height: 1.05; margin-bottom: 8px; }
.hero-text .pron { font-size: 15px; color: var(--ink-mute); font-family: ui-monospace, 'SF Mono', Menlo, monospace; margin-bottom: 14px; }
.hero-meta { display: flex; flex-wrap: wrap; gap: 6px; }
.tag { display: inline-flex; align-items: center; font-size: 12px; font-weight: 500; padding: 4px 10px; border-radius: 999px; background: var(--accent-soft); color: var(--accent); cursor: pointer; border: none; font-family: inherit; }
.tag:hover { background: var(--accent); color: white; }
.hero-arab { font-family: var(--arab); font-size: 88px; color: var(--ink); line-height: 1; direction: rtl; text-align: right; font-weight: 400; }
@media (max-width: 600px) { .hero-arab { font-size: 64px; text-align: center; } .hero-text h1 { font-size: 36px; } }
.quick-stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; padding-top: 24px; border-top: 1px solid var(--border-soft); }
@media (max-width: 700px) { .quick-stats { grid-template-columns: 1fr; } }
.qstat { display: grid; gap: 4px; }
.qstat-label { font-size: 11px; font-weight: 600; letter-spacing: 0.05em; text-transform: uppercase; color: var(--ink-mute); }
.qstat-value { font-size: 14px; font-weight: 500; color: var(--ink); }
.qstat-value .ar { font-family: var(--arab); font-size: 17px; margin-right: 4px; }

.toolbar { display: flex; align-items: center; justify-content: space-between; gap: 16px; padding: 16px 4px 4px; }
.toolbar-title { font-size: 13px; font-weight: 600; letter-spacing: 0.04em; text-transform: uppercase; color: var(--ink-mute); }
.filter-pills { display: flex; gap: 4px; background: var(--border-soft); padding: 3px; border-radius: 10px; }
.filter-pills button { border: none; background: transparent; font-family: inherit; font-size: 12px; font-weight: 600; padding: 6px 12px; border-radius: 7px; cursor: pointer; color: var(--ink-soft); }
.filter-pills button.active { background: var(--card); color: var(--ink); box-shadow: var(--shadow-sm); }

.modules { display: grid; gap: 16px; }
.mod-card { background: var(--card); border-radius: var(--radius); padding: 24px; box-shadow: var(--shadow-sm); border: 1px solid var(--border-soft); transition: box-shadow 0.2s; scroll-margin-top: 80px; }
.mod-card:hover { box-shadow: var(--shadow-md); }
.mod-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-bottom: 16px; }
.mod-head-left { display: flex; align-items: center; gap: 14px; }
.mod-num { width: 32px; height: 32px; background: var(--accent-soft); color: var(--accent); border-radius: 10px; display: grid; place-items: center; font-size: 14px; font-weight: 700; }
.mod-title { font-size: 17px; font-weight: 600; letter-spacing: -0.015em; }
.mod-badge { font-size: 10px; font-weight: 700; padding: 4px 9px; border-radius: 6px; letter-spacing: 0.06em; text-transform: uppercase; }
.mod-badge.pflicht { background: var(--pflicht-bg); color: var(--pflicht-fg); }
.mod-badge.option { background: var(--option-bg); color: var(--option-fg); }
.mod-body { font-size: 15px; line-height: 1.6; color: var(--ink); }
.mod-body p { margin-bottom: 12px; }
.mod-body p:last-child { margin-bottom: 0; }
.mod-body strong { font-weight: 600; color: var(--ink); }
.term { font-style: italic; font-weight: 500; }
.translation { background: var(--option-bg); color: var(--option-fg); padding: 1px 6px; border-radius: 4px; font-weight: 500; font-size: 0.95em; white-space: nowrap; }
.ar-inline { font-family: var(--arab); font-size: 1.2em; vertical-align: -1px; direction: rtl; display: inline-block; unicode-bidi: isolate; }
body.filter-pflicht .mod-card[data-type="option"] { display: none; }

.form-grid { display: grid; grid-template-columns: max-content 1fr; gap: 10px 20px; font-size: 14px; }
.form-grid dt { color: var(--ink-mute); font-weight: 500; }
.form-grid dd .arab { font-family: var(--arab); font-size: 22px; line-height: 1; direction: rtl; }
.form-grid dd .mono { font-family: ui-monospace, 'SF Mono', Menlo, monospace; font-size: 13px; background: var(--card-soft); padding: 2px 6px; border-radius: 4px; }

.quran-card { background: linear-gradient(135deg, #FAFBFD 0%, #F0F2F8 100%); border-radius: 12px; padding: 16px 18px; margin: 14px 0; border-left: 3px solid var(--accent); }
.quran-label { display: inline-block; font-size: 10px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; padding: 3px 8px; background: var(--accent); color: white; border-radius: 5px; margin-bottom: 10px; }
.quran-text { font-style: italic; font-size: 15px; line-height: 1.55; }

.equiv-list { display: grid; gap: 10px; margin-top: 12px; }
.equiv-row { display: grid; grid-template-columns: 180px 1fr auto; gap: 16px; padding: 14px 16px; background: var(--card-soft); border-radius: 10px; align-items: center; font-size: 14px; }
@media (max-width: 700px) { .equiv-row { grid-template-columns: 1fr; gap: 8px; } }
.equiv-name { font-style: italic; font-weight: 600; }
.equiv-desc { color: var(--ink-soft); line-height: 1.5; }
.equiv-rating { font-size: 11px; font-weight: 700; padding: 4px 10px; border-radius: 6px; letter-spacing: 0.04em; text-transform: uppercase; white-space: nowrap; text-align: center; }
.equiv-rating.good { background: var(--rating-good-bg); color: var(--rating-good-fg); }
.equiv-rating.mid { background: var(--rating-mid-bg); color: var(--rating-mid-fg); }
.equiv-rating.bad { background: var(--rating-bad-bg); color: var(--rating-bad-fg); }

.examples { display: grid; gap: 12px; margin-top: 8px; }
.ex-card { background: var(--card-soft); border-radius: 10px; padding: 14px 16px; border: 1px solid var(--border-soft); }
.ex-context { display: inline-block; font-size: 10px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: var(--accent); margin-bottom: 8px; }
.ex-body { font-size: 14px; line-height: 1.55; }

.aside { display: grid; gap: 16px; align-content: start; }
@media (min-width: 1000px) { .aside { position: sticky; top: 80px; align-self: start; } }
.aside-card { background: var(--card); border-radius: var(--radius); padding: 18px 20px; box-shadow: var(--shadow-sm); border: 1px solid var(--border-soft); }
.aside-title { font-size: 11px; font-weight: 700; letter-spacing: 0.06em; text-transform: uppercase; color: var(--ink-mute); margin-bottom: 14px; }
.nav-list { list-style: none; display: grid; gap: 2px; }
.nav-list button { display: flex; align-items: center; gap: 10px; padding: 7px 9px; border-radius: 7px; background: none; border: none; cursor: pointer; width: 100%; text-align: left; font-family: inherit; color: var(--ink-soft); font-size: 13px; font-weight: 500; }
.nav-list button:hover { background: var(--accent-soft); color: var(--accent); }
.nav-list .nav-num { width: 22px; height: 22px; display: grid; place-items: center; font-size: 10px; font-weight: 700; background: var(--border-soft); border-radius: 5px; color: var(--ink-mute); }
.nav-list button:hover .nav-num { background: var(--accent); color: white; }
.nav-list .nav-dot { width: 5px; height: 5px; border-radius: 50%; background: var(--ink-mute); margin-left: auto; opacity: 0.6; }
.nav-list .nav-dot.pflicht { background: var(--accent); opacity: 1; }
.nav-list .nav-arab { font-family: var(--arab); font-size: 13px; width: 22px; height: 22px; display: grid; place-items: center; background: var(--accent-soft); color: var(--accent); border-radius: 5px; }
.nav-list button.placeholder { opacity: 0.6; }
.nav-list button.placeholder .nav-arab { background: var(--border-soft); color: var(--ink-mute); }
.nav-list .pending-tag { font-size: 9px; padding: 1px 5px; background: var(--border-soft); color: var(--ink-mute); border-radius: 4px; margin-left: auto; }

.connect-group { margin-bottom: 20px; }
.connect-group:last-child { margin-bottom: 0; }
.connect-label { font-size: 11px; font-weight: 700; letter-spacing: 0.06em; text-transform: uppercase; color: var(--ink-mute); margin-bottom: 10px; }
.connect-chips { display: flex; flex-wrap: wrap; gap: 8px; }
.chip { display: inline-flex; align-items: center; gap: 8px; padding: 7px 12px; background: var(--card-soft); border: 1px solid var(--border); border-radius: 10px; cursor: pointer; font-size: 13px; color: var(--ink); font-family: inherit; }
.chip:hover { border-color: var(--accent); background: var(--accent-soft); transform: translateY(-1px); box-shadow: var(--shadow-sm); }
.chip .ar { font-family: var(--arab); font-size: 17px; line-height: 1; }
.chip .name { font-style: italic; font-weight: 500; }
.chip .desc { font-size: 11px; color: var(--ink-mute); border-left: 1px solid var(--border); padding-left: 8px; }
.path-card { background: linear-gradient(135deg, #EEF0FF 0%, #F5F0FF 100%); border-radius: var(--radius); padding: 20px; border: 1px solid var(--border-soft); }
.path-flow { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; margin-top: 12px; }
.path-node { font-style: italic; font-weight: 600; padding: 8px 14px; background: var(--card); border-radius: 10px; font-size: 14px; box-shadow: var(--shadow-sm); border: none; cursor: pointer; font-family: inherit; }
.path-node:hover { transform: translateY(-1px); }
.path-node.current { background: var(--accent); color: white; }
.path-arrow { color: var(--accent); font-weight: 700; }

.lemma-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 16px; }
.lemma-card { background: var(--card); border-radius: var(--radius); padding: 22px; box-shadow: var(--shadow-sm); border: 1px solid var(--border-soft); cursor: pointer; transition: all 0.2s; display: flex; flex-direction: column; gap: 14px; text-align: left; font-family: inherit; color: inherit; }
.lemma-card:hover { box-shadow: var(--shadow-lg); transform: translateY(-2px); border-color: var(--accent); }
.lemma-card-top { display: flex; justify-content: space-between; align-items: flex-start; gap: 10px; }
.lemma-card-arab { font-family: var(--arab); font-size: 40px; line-height: 1; color: var(--ink); direction: rtl; }
.lemma-card-status { font-size: 10px; font-weight: 700; padding: 4px 9px; border-radius: 6px; letter-spacing: 0.06em; text-transform: uppercase; }
.lemma-card-status.ready { background: var(--rating-good-bg); color: var(--rating-good-fg); }
.lemma-card-status.pending { background: var(--border-soft); color: var(--ink-mute); }
.lemma-card-name { font-size: 22px; font-weight: 700; letter-spacing: -0.02em; }
.lemma-card-trans { font-size: 14px; color: var(--ink-soft); line-height: 1.5; }
.lemma-card-fields { display: flex; flex-wrap: wrap; gap: 4px; margin-top: auto; padding-top: 8px; border-top: 1px solid var(--border-soft); }
.lemma-card-field { font-size: 11px; color: var(--ink-mute); padding: 2px 7px; background: var(--card-soft); border-radius: 999px; }

.themes-grid { display: grid; gap: 16px; }
.theme-card { background: var(--card); border-radius: var(--radius); padding: 22px 24px; box-shadow: var(--shadow-sm); border: 1px solid var(--border-soft); }
.theme-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-bottom: 16px; padding-bottom: 14px; border-bottom: 1px solid var(--border-soft); }
.theme-title { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; }
.theme-count { font-size: 12px; font-weight: 600; color: var(--ink-mute); background: var(--card-soft); padding: 3px 10px; border-radius: 999px; }
.theme-desc { font-size: 14px; color: var(--ink-soft); margin-bottom: 16px; line-height: 1.55; }
.theme-lemmata { display: flex; flex-wrap: wrap; gap: 8px; }

.path-list { display: grid; gap: 16px; }
.path-card-large { background: var(--card); border-radius: var(--radius); padding: 24px; box-shadow: var(--shadow-sm); border: 1px solid var(--border-soft); }
.path-card-large-head { display: flex; justify-content: space-between; align-items: flex-start; gap: 14px; margin-bottom: 12px; }
.path-card-title { font-size: 18px; font-weight: 700; letter-spacing: -0.01em; }
.path-card-large-desc { font-size: 14px; color: var(--ink-soft); line-height: 1.55; margin-bottom: 16px; }
.path-card-large-flow { display: flex; align-items: center; gap: 6px; flex-wrap: wrap; }

.placeholder-card { background: var(--card); border-radius: var(--radius-lg); padding: 64px 40px; box-shadow: var(--shadow-md); text-align: center; max-width: 600px; margin: 60px auto; }
.placeholder-arab { font-family: var(--arab); font-size: 96px; line-height: 1; color: var(--ink); margin-bottom: 20px; direction: rtl; }
.placeholder-name { font-size: 36px; font-weight: 700; letter-spacing: -0.02em; margin-bottom: 8px; }
.placeholder-trans { font-size: 16px; color: var(--ink-soft); margin-bottom: 28px; }
.placeholder-status { display: inline-flex; align-items: center; gap: 8px; padding: 10px 18px; background: var(--option-bg); color: var(--option-fg); border-radius: 999px; font-size: 13px; font-weight: 600; margin-bottom: 24px; }
.placeholder-text { font-size: 14px; color: var(--ink-soft); line-height: 1.6; max-width: 460px; margin: 0 auto 24px; }
.placeholder-back { display: inline-flex; align-items: center; gap: 6px; padding: 10px 18px; background: var(--accent); color: white; border-radius: 10px; border: none; font-family: inherit; font-size: 14px; font-weight: 600; cursor: pointer; }
.placeholder-back:hover { background: var(--accent-hover); transform: translateY(-1px); }

.footer { margin-top: 40px; padding: 20px 0; font-size: 12px; color: var(--ink-mute); text-align: center; }
</style>
</head>
<body>

<header class="topbar">
  <div class="topbar-inner">
    <button class="brand" onclick="goView('lexikon')">
      <div class="brand-mark">م</div>
      <span>Miftāḥ</span>
    </button>
    <div class="search-bar">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
      <input type="text" id="search-input" placeholder="Begriff suchen…" oninput="onSearch(this.value)" onfocus="onSearch(this.value)" onblur="setTimeout(closeSearch, 200)">
      <div class="search-dropdown" id="search-dropdown"></div>
    </div>
    <nav class="topbar-nav">
      <button id="nav-lexikon" onclick="goView('lexikon')">Lexikon</button>
      <button id="nav-themen" onclick="goView('themen')">Themen</button>
      <button id="nav-pfade" onclick="goView('pfade')">Lernpfade</button>
    </nav>
  </div>
</header>

<div class="shell">

  <div class="notice-banner">
    <strong>Prototyp-Status:</strong>&nbsp;Diese Vorschauversion zeigt die vollständige Architektur des Wörterbuchs. Inhaltlich ist nur das Lemma <em>Taqwā</em> ausgearbeitet — die anderen fünf Lemmata sind als Platzhalter sichtbar und werden in der Endversion mit Inhalt befüllt.
  </div>

  <section class="view active" id="view-lexikon">
    <div class="view-header">
      <h2 class="view-title">Lexikon</h2>
      <p class="view-subtitle">Sechs Schlüsselbegriffe im Initialkorpus, alphabetisch geordnet</p>
    </div>
    <div class="lemma-grid" id="lemma-grid"></div>
  </section>

  <section class="view" id="view-themen">
    <div class="view-header">
      <h2 class="view-title">Themen</h2>
      <p class="view-subtitle">Semantische Felder mit Zuordnung der Lemmata</p>
    </div>
    <div class="themes-grid" id="themes-grid"></div>
  </section>

  <section class="view" id="view-pfade">
    <div class="view-header">
      <h2 class="view-title">Lernpfade</h2>
      <p class="view-subtitle">Kuratierte Folgen von Lemmata für didaktisches Lernen</p>
    </div>
    <div class="path-list" id="path-list"></div>
  </section>

  <section class="view" id="view-detail">
    <div id="detail-content"></div>
  </section>

  <footer class="footer">
    Miftāḥ · Prototyp eines erklärend-enzyklopädischen Wörterbuchs · Ahmed Shaita 2026
  </footer>

</div>

<script>
const lemmata = {
  taqwa: { id: 'taqwa', arab: 'تَقْوَى', name: 'Taqwā', pron: '[taqˈwaː]', grammar: 'Nomen femininum', root: 'و-ق-ي', rootLatin: 'w-q-y', coreTranslation: 'Gottesbewusstsein', fields: ['Spiritualität', 'Glaubensethik', 'Quranische Begriffe'], ready: true },
  zakat: { id: 'zakat', arab: 'زَكَاة', name: 'Zakāt', pron: '[zaˈkaːh]', grammar: 'Nomen femininum', root: 'ز-ك-ي', rootLatin: 'z-k-y', coreTranslation: 'Pflichtabgabe', fields: ['Rituelle Praxis', 'Rechtsterminologie'], ready: false },
  iman: { id: 'iman', arab: 'إِيمَان', name: 'Īmān', pron: '[ʔiːˈmaːn]', grammar: 'Nomen maskulinum', root: 'ء-م-ن', rootLatin: 'ʾ-m-n', coreTranslation: 'Glaube', fields: ['Glaubenslehre', 'Spiritualität', 'Ethik'], ready: false },
  sadaqa: { id: 'sadaqa', arab: 'صَدَقَة', name: 'Ṣadaqa', pron: '[sˤaˈdaqa]', grammar: 'Nomen femininum', root: 'ص-د-ق', rootLatin: 'ṣ-d-q', coreTranslation: 'Freiwillige Spende', fields: ['Rituelle Praxis', 'Ethik'], ready: false },
  salah: { id: 'salah', arab: 'صَلَاة', name: 'Ṣalāh', pron: '[sˤaˈlaːh]', grammar: 'Nomen femininum', root: 'ص-ل-و', rootLatin: 'ṣ-l-w', coreTranslation: 'Pflichtgebet', fields: ['Rituelle Praxis'], ready: false },
  din: { id: 'din', arab: 'دِين', name: 'Dīn', pron: '[diːn]', grammar: 'Nomen maskulinum', root: 'د-ي-ن', rootLatin: 'd-y-n', coreTranslation: 'Religion / Lebensweise', fields: ['Glaubenslehre'], ready: false }
};

const themes = [
  { title: 'Innere Spiritualität', desc: 'Begriffe, die das innere Verhältnis des Gläubigen zu Gott und die Disposition des Herzens beschreiben.', lemmata: ['taqwa', 'iman'] },
  { title: 'Rituelle Praxis', desc: 'Begriffe der gottesdienstlichen Handlungen und Pflichten in der gelebten Glaubenspraxis.', lemmata: ['salah', 'zakat', 'sadaqa'] },
  { title: 'Glaubenslehre', desc: 'Grundlegende theologische Konzepte, die das Selbstverständnis des Islams als Religion und Lebensweise tragen.', lemmata: ['iman', 'din'] },
  { title: 'Ethik und Tugendlehre', desc: 'Begriffe, die das sittlich-praktische Handeln und die Tugendvorstellungen des Islams strukturieren.', lemmata: ['taqwa', 'iman', 'sadaqa'] },
  { title: 'Rechtsterminologie (fiqh)', desc: 'Begriffe, die im islamischen Recht eine spezifische, oft normativ ausgearbeitete Bedeutung tragen.', lemmata: ['zakat'] }
];

const paths = [
  { title: 'Innere Disposition', desc: 'Eine didaktische Sequenz von der grundlegenden Haltung zur verfeinerten spirituellen Disposition.', sequence: ['taqwa', 'iman'], extended: ['Iḥsān', 'Khushūʿ'] },
  { title: 'Geben im Islam', desc: 'Der Themenkomplex des Spendens, beginnend bei der verpflichtenden Abgabe und ihrer Abgrenzung zum freiwilligen Geben.', sequence: ['zakat', 'sadaqa'], extended: [] },
  { title: 'Die Säulen der Praxis', desc: 'Die zentralen praktischen Pflichten des Islams als zusammenhängender Lernbogen.', sequence: ['salah', 'zakat'], extended: ['Ṣawm (Fasten)', 'Ḥajj (Pilgerfahrt)'] }
];

function goView(view, opts) {
  document.querySelectorAll('.view').forEach(v => v.classList.remove('active'));
  document.querySelectorAll('.topbar-nav button').forEach(b => b.classList.remove('active'));
  if (view === 'lexikon') { document.getElementById('view-lexikon').classList.add('active'); document.getElementById('nav-lexikon').classList.add('active'); renderLexikon(); }
  else if (view === 'themen') { document.getElementById('view-themen').classList.add('active'); document.getElementById('nav-themen').classList.add('active'); renderThemen(); }
  else if (view === 'pfade') { document.getElementById('view-pfade').classList.add('active'); document.getElementById('nav-pfade').classList.add('active'); renderPfade(); }
  else if (view === 'detail') { document.getElementById('view-detail').classList.add('active'); renderDetail(opts.lemmaId); }
  window.scrollTo({ top: 0, behavior: 'smooth' });
}

function renderLexikon() {
  const sorted = Object.values(lemmata).sort((a, b) => a.name.localeCompare(b.name));
  document.getElementById('lemma-grid').innerHTML = sorted.map(l => `
    <button class="lemma-card" onclick="goView('detail', {lemmaId: '${l.id}'})">
      <div class="lemma-card-top">
        <div class="lemma-card-arab">${l.arab}</div>
        <span class="lemma-card-status ${l.ready ? 'ready' : 'pending'}">${l.ready ? '✓ Verfügbar' : 'In Vorbereitung'}</span>
      </div>
      <div>
        <div class="lemma-card-name"><em>${l.name}</em></div>
        <div class="lemma-card-trans">${l.coreTranslation}</div>
      </div>
      <div class="lemma-card-fields">${l.fields.map(f => `<span class="lemma-card-field">${f}</span>`).join('')}</div>
    </button>`).join('');
}

function renderThemen() {
  document.getElementById('themes-grid').innerHTML = themes.map(t => `
    <div class="theme-card">
      <div class="theme-head">
        <h3 class="theme-title">${t.title}</h3>
        <span class="theme-count">${t.lemmata.length} Lemmata</span>
      </div>
      <p class="theme-desc">${t.desc}</p>
      <div class="theme-lemmata">${t.lemmata.map(lid => {
        const l = lemmata[lid];
        return `<button class="chip" onclick="goView('detail', {lemmaId: '${l.id}'})">
          <span class="ar">${l.arab}</span><span class="name">${l.name}</span><span class="desc">${l.coreTranslation}</span>
        </button>`;
      }).join('')}</div>
    </div>`).join('');
}

function renderPfade() {
  document.getElementById('path-list').innerHTML = paths.map(p => `
    <div class="path-card-large">
      <div class="path-card-large-head">
        <h3 class="path-card-title">${p.title}</h3>
        <span class="theme-count">${p.sequence.length + p.extended.length} Stationen</span>
      </div>
      <p class="path-card-large-desc">${p.desc}</p>
      <div class="path-card-large-flow">
        ${p.sequence.map((lid, i) => {
          const l = lemmata[lid];
          const arrow = (i < p.sequence.length - 1 || p.extended.length > 0) ? '<span class="path-arrow">→</span>' : '';
          return `<button class="path-node" onclick="goView('detail', {lemmaId: '${l.id}'})">${l.name}</button>${arrow}`;
        }).join('')}
        ${p.extended.map((ext, i) => {
          const arrow = i < p.extended.length - 1 ? '<span class="path-arrow">→</span>' : '';
          return `<span class="path-node" style="opacity: 0.6;">${ext}</span>${arrow}`;
        }).join('')}
      </div>
    </div>`).join('');
}

function renderModule(num, title, type, body) {
  return `<article class="mod-card" data-type="${type}" id="m${num}">
    <div class="mod-head">
      <div class="mod-head-left"><div class="mod-num">${num}</div><h3 class="mod-title">${title}</h3></div>
      <span class="mod-badge ${type}">${type === 'pflicht' ? 'Pflicht' : 'Option'}</span>
    </div>
    <div class="mod-body">${body}</div>
  </article>`;
}

function renderDetail(lemmaId) {
  const l = lemmata[lemmaId];
  const container = document.getElementById('detail-content');

  if (!l.ready) {
    container.innerHTML = `
      <div class="breadcrumb">
        <button onclick="goView('lexikon')">Lexikon</button>
        <span class="sep">/</span><span>${l.name}</span>
      </div>
      <div class="placeholder-card">
        <div class="placeholder-arab">${l.arab}</div>
        <div class="placeholder-name">${l.name}</div>
        <div class="placeholder-trans">${l.coreTranslation}</div>
        <div class="placeholder-status">⏳ Eintrag in Vorbereitung</div>
        <p class="placeholder-text">Dieser Eintrag wird in der vollständigen Version mit allen acht Modulen ausgearbeitet: Form und Erschließung, Kernbedeutung, Bedeutungsvarianten, kulturhistorischer Kontext, Gebrauchspragmatik, Äquivalenz und Übersetzung, Beispiele sowie Vernetzung.</p>
        <button class="placeholder-back" onclick="goView('lexikon')">← Zurück zum Lexikon</button>
      </div>`;
    return;
  }

  const otherLemmata = Object.values(lemmata).filter(x => x.id !== 'taqwa');

  container.innerHTML = `
    <div class="breadcrumb">
      <button onclick="goView('lexikon')">Lexikon</button>
      <span class="sep">/</span><span>Taqwā</span>
    </div>
    <div class="with-aside">
      <main>
        <section class="hero">
          <div class="hero-main">
            <div class="hero-text">
              <h1>Taqwā</h1>
              <div class="pron">[taqˈwaː] · Nomen femininum</div>
              <div class="hero-meta">
                <button class="tag" onclick="goView('themen')">Spiritualität</button>
                <button class="tag" onclick="goView('themen')">Glaubensethik</button>
                <button class="tag" onclick="goView('themen')">Quranische Begriffe</button>
              </div>
            </div>
            <div class="hero-arab">تَقْوَى</div>
          </div>
          <div class="quick-stats">
            <div class="qstat"><span class="qstat-label">Wurzel</span><span class="qstat-value"><span class="ar">و-ق-ي</span>w-q-y</span></div>
            <div class="qstat"><span class="qstat-label">Kernbedeutung</span><span class="qstat-value">Gottesbewusstsein</span></div>
            <div class="qstat"><span class="qstat-label">Verbalform</span><span class="qstat-value">VIII (ittaqā)</span></div>
          </div>
        </section>

        <div class="toolbar">
          <span class="toolbar-title">Module · 8</span>
          <div class="filter-pills">
            <button class="active" onclick="setFilter('alle', this)">Alle</button>
            <button onclick="setFilter('pflicht', this)">Nur Pflicht</button>
          </div>
        </div>

        <div class="modules">
          ${renderModule(1, 'Form und Erschließung', 'pflicht', `
            <dl class="form-grid">
              <dt>Arabisch</dt><dd><span class="arab">تَقْوَى</span></dd>
              <dt>Umschrift (DMG)</dt><dd><span class="term">Taqwā</span></dd>
              <dt>Aussprache</dt><dd><span class="mono">[taqˈwaː]</span></dd>
              <dt>Grammatik</dt><dd>Nomen femininum; Verbalnomen (<span class="term">maṣdar</span>) der achten Verbalform</dd>
              <dt>Wurzel</dt><dd><span class="ar-inline">و-ق-ي</span> (w-q-y) · Grundbedeutung: schützen, bewahren, hüten</dd>
            </dl>`)}
          ${renderModule(2, 'Kernbedeutung', 'pflicht', `
            <p><span class="term">Taqwā</span> bezeichnet eine bewusste, durch Demut und Achtsamkeit gegenüber Gott geprägte Lebenshaltung. Die geläufige Übersetzung <span class="translation">„Gottesfurcht"</span> erfasst nur einen Teilaspekt und kann durch die christliche Konnotation der Furcht in die Irre führen. Präziser sind <span class="translation">„Gottesbewusstsein"</span> oder <span class="translation">„Achtsamkeit gegenüber Gott"</span>; siehe für eine ausführliche Diskussion Modul 6.</p>`)}
          ${renderModule(3, 'Bedeutungsvarianten', 'pflicht', `
            <p><span class="term">Taqwā</span> trägt mehrere Bedeutungsebenen, die sich in den Quellen unterschiedlich gewichten lassen.</p>
            <p><strong>Theologisch:</strong> die innere Haltung der Verantwortung gegenüber Gott, die in jedem Augenblick präsent ist.</p>
            <p><strong>Ethisch:</strong> die Disposition, die zwischen Erlaubtem und Verbotenem unterscheidet und zu rechtschaffenem Handeln führt.</p>
            <p><strong>Spirituell-mystisch:</strong> in der sufischen Tradition der höchste Grad geistiger Wachsamkeit, in dem das Bewusstsein des Göttlichen das Tun durchdringt.</p>
            <p>Diese Ebenen sind nicht trennscharf; sie überlagern sich häufig im selben Vorkommen.</p>`)}
          ${renderModule(4, 'Kulturhistorischer Kontext', 'option', `
            <p><span class="term">Taqwā</span> gehört zu den am häufigsten genannten Tugenden im Quran.</p>
            <div class="quran-card"><div class="quran-label">Quran 49:13</div><div class="quran-text">„Der Edelste von euch bei Gott ist derjenige unter euch, der die größte Taqwā hat."</div></div>
            <p>Hier wird <span class="term">Taqwā</span> zum einzig legitimen Kriterium menschlicher Auszeichnung erklärt, jenseits von Herkunft, Stamm oder Geschlecht.</p>
            <div class="quran-card"><div class="quran-label">Quran 2:2–3</div><div class="quran-text">Der Quran wird als „Rechtleitung für die Gottesbewussten (al-muttaqīn)" eingeführt — Taqwā ist hier Voraussetzung für das Verstehen der Offenbarung.</div></div>
            <div class="quran-card"><div class="quran-label">Quran 2:183</div><div class="quran-text">Im Fastengebot wird Taqwā als Ziel des Fastens benannt: „auf dass ihr Taqwā erlangt".</div></div>
            <p>In der prophetischen Tradition steht der bekannte Hadith, in dem der Prophet auf sein Herz deutet und sagt: „Die Frömmigkeit ist hier" (überliefert bei Muslim). Bei al-Ghazālī (gest. 1111) gilt <span class="term">Taqwā</span> als die Wurzel aller anderen Tugenden.</p>`)}
          ${renderModule(5, 'Gebrauchspragmatik', 'option', `
            <p>In modernen Predigten gehört <span class="term">Taqwā</span> zum festen Vokabular; in Gesprächen unter Muslimen wird die Frage „Hast du Taqwā?" oft als ethische Erinnerung verwendet, vergleichbar mit dem deutschen „Sei achtsam".</p>
            <p>Deutsche Wiedergaben variieren stark: <span class="translation">„Gottesfurcht"</span>, <span class="translation">„Gottesbewusstsein"</span>, <span class="translation">„Gottesehrfurcht"</span> und <span class="translation">„Frömmigkeit"</span> werden parallel benutzt. Diese Vielfalt ist kein Zufall, sondern Ausdruck der Schwierigkeit, den Begriff in einer einzigen deutschen Vokabel zu fassen.</p>
            <p><strong>Typische Missverständnisse:</strong> <span class="term">Taqwā</span> wird gelegentlich mit reiner Angst, mit Schuldbewusstsein oder mit asketischer Strenge verwechselt. Im arabischen Konzept liegt das Gewicht jedoch nicht auf der Angst, sondern auf der Wachsamkeit.</p>`)}
          ${renderModule(6, 'Äquivalenz und Übersetzung', 'option', `
            <p>Die deutsche Wiedergabe von <span class="term">Taqwā</span> steht beispielhaft für das in dieser Konzeption zentrale Äquivalenzproblem.</p>
            <div class="equiv-list">
              <div class="equiv-row"><span class="equiv-name">Gottesfurcht</span><span class="equiv-desc">trifft Ehrfurcht, übernimmt aber die christliche Furcht-Konnotation</span><span class="equiv-rating bad">Missverständlich</span></div>
              <div class="equiv-row"><span class="equiv-name">Gottesbewusstsein</span><span class="equiv-desc">trifft Bewusstheit, weniger die ethische Verbindlichkeit</span><span class="equiv-rating mid">Brauchbar</span></div>
              <div class="equiv-row"><span class="equiv-name">Gottesehrfurcht</span><span class="equiv-desc">balanciert Ehrfurcht und Wachsamkeit</span><span class="equiv-rating good">Idiomatisch</span></div>
              <div class="equiv-row"><span class="equiv-name">Achtsamkeit gegenüber Gott</span><span class="equiv-desc">umschreibend, präzise, aber sperrig</span><span class="equiv-rating good">Analytisch klar</span></div>
            </div>
            <p>Im Sinne der Äquivalenztypologie (Zgusta, 1971) handelt es sich in keinem Fall um Volläquivalenz. Die Wurzel <span class="ar-inline">و-ق-ي</span> trägt nicht primär die Bedeutung des „Fürchtens", sondern die des „Hütens" und „Bewahrens": <span class="term">Taqwā</span> ist im Kern eine <em>behütende</em> Haltung.</p>`)}
          ${renderModule(7, 'Beispiele und Szenarien', 'option', `
            <p>Drei kontrastive Szenarien zeigen, in welchen Kontexten welche Bedeutungsfacette aktiviert wird.</p>
            <div class="examples">
              <div class="ex-card"><span class="ex-context">Im Gebet</span><div class="ex-body"><span class="term">Taqwā</span> meint die innere Wachsamkeit dafür, dass man vor Gott steht — nicht das Auswendiglernen der Bewegungen, sondern die Präsenz des Bewusstseins im Vollzug.</div></div>
              <div class="ex-card"><span class="ex-context">Im sozialen Handeln</span><div class="ex-body"><span class="term">Taqwā</span> zeigt sich als ethischer Anker im Umgang mit anderen, etwa in der Verlässlichkeit beim Versprechen.</div></div>
              <div class="ex-card"><span class="ex-context">Im Ramadan</span><div class="ex-body">Das Fasten ist nach Quran 2:183 selbst ein Übungsfeld für <span class="term">Taqwā</span>; das Aussetzen sinnlicher Reize schult die Wachsamkeit für den Alltag.</div></div>
            </div>`)}
          ${renderModule(8, 'Vernetzung', 'option', `
            <div class="connect-group">
              <div class="connect-label">Kohyponyme · Verwandte religiös-ethische Begriffe</div>
              <div class="connect-chips">
                <button class="chip" onclick="goView('detail', {lemmaId: 'iman'})"><span class="ar">إِيمَان</span><span class="name">Īmān</span><span class="desc">Glaube</span></button>
                <span class="chip" style="opacity: 0.5; cursor: default;"><span class="ar">إِحْسَان</span><span class="name">Iḥsān</span><span class="desc">Erweiterung</span></span>
                <span class="chip" style="opacity: 0.5; cursor: default;"><span class="ar">خُشُوع</span><span class="name">Khushūʿ</span><span class="desc">Erweiterung</span></span>
              </div>
            </div>
            <div class="connect-group">
              <div class="connect-label">Antonym</div>
              <div class="connect-chips">
                <span class="chip" style="opacity: 0.5; cursor: default;"><span class="ar">غَفْلَة</span><span class="name">Ghafla</span><span class="desc">Erweiterung</span></span>
              </div>
            </div>
            <div class="connect-group">
              <div class="connect-label">Etymologische Verwandte · Wurzel w-q-y</div>
              <div class="connect-chips">
                <span class="chip" style="opacity: 0.5; cursor: default;"><span class="ar">مُتَّقِي</span><span class="name">Muttaqī</span><span class="desc">der Gottesbewusste</span></span>
                <span class="chip" style="opacity: 0.5; cursor: default;"><span class="ar">اِتِّقَاء</span><span class="name">Ittiqāʾ</span><span class="desc">das Sich-Hüten</span></span>
              </div>
            </div>
            <div class="path-card">
              <div class="connect-label">Lernpfad · Innere Spiritualität</div>
              <div class="path-flow">
                <button class="path-node current">Taqwā</button>
                <span class="path-arrow">→</span>
                <button class="path-node" onclick="goView('detail', {lemmaId: 'iman'})">Īmān</button>
              </div>
            </div>`)}
        </div>
      </main>

      <aside class="aside">
        <div class="aside-card">
          <div class="aside-title">Module · Sprungnavigation</div>
          <ol class="nav-list">
            <li><button onclick="document.getElementById('m1').scrollIntoView()"><span class="nav-num">1</span>Form und Erschließung<span class="nav-dot pflicht"></span></button></li>
            <li><button onclick="document.getElementById('m2').scrollIntoView()"><span class="nav-num">2</span>Kernbedeutung<span class="nav-dot pflicht"></span></button></li>
            <li><button onclick="document.getElementById('m3').scrollIntoView()"><span class="nav-num">3</span>Bedeutungsvarianten<span class="nav-dot pflicht"></span></button></li>
            <li><button onclick="document.getElementById('m4').scrollIntoView()"><span class="nav-num">4</span>Kulturhistorischer Kontext<span class="nav-dot"></span></button></li>
            <li><button onclick="document.getElementById('m5').scrollIntoView()"><span class="nav-num">5</span>Gebrauchspragmatik<span class="nav-dot"></span></button></li>
            <li><button onclick="document.getElementById('m6').scrollIntoView()"><span class="nav-num">6</span>Äquivalenz<span class="nav-dot"></span></button></li>
            <li><button onclick="document.getElementById('m7').scrollIntoView()"><span class="nav-num">7</span>Beispiele<span class="nav-dot"></span></button></li>
            <li><button onclick="document.getElementById('m8').scrollIntoView()"><span class="nav-num">8</span>Vernetzung<span class="nav-dot"></span></button></li>
          </ol>
        </div>
        <div class="aside-card">
          <div class="aside-title">Andere Lemmata</div>
          <ol class="nav-list">
            ${otherLemmata.map(o => `<li><button class="${o.ready ? '' : 'placeholder'}" onclick="goView('detail', {lemmaId: '${o.id}'})">
              <span class="nav-arab">${o.arab.charAt(0)}</span>${o.name}${o.ready ? '' : '<span class="pending-tag">Vorber.</span>'}
            </button></li>`).join('')}
          </ol>
        </div>
      </aside>
    </div>`;
}

function setFilter(mode, button) {
  document.body.classList.remove('filter-alle', 'filter-pflicht');
  document.body.classList.add('filter-' + mode);
  button.parentElement.querySelectorAll('button').forEach(b => b.classList.remove('active'));
  button.classList.add('active');
}

function onSearch(q) {
  const dropdown = document.getElementById('search-dropdown');
  q = q.trim().toLowerCase();
  let matches = Object.values(lemmata);
  if (q.length > 0) {
    matches = matches.filter(l => l.name.toLowerCase().includes(q) || l.coreTranslation.toLowerCase().includes(q) || l.arab.includes(q));
  }
  if (matches.length === 0) {
    dropdown.innerHTML = '<div class="search-dropdown-empty">Keine Treffer</div>';
  } else {
    dropdown.innerHTML = matches.map(l => `
      <div class="search-item" onmousedown="goView('detail', {lemmaId: '${l.id}'}); closeSearch(); document.getElementById('search-input').value='';">
        <span class="ar">${l.arab}</span>
        <div class="search-item-meta">
          <span class="search-item-name">${l.name}</span>
          <span class="search-item-trans">${l.coreTranslation}${l.ready ? '' : ' · in Vorbereitung'}</span>
        </div>
      </div>`).join('');
  }
  dropdown.classList.add('open');
}

function closeSearch() {
  document.getElementById('search-dropdown').classList.remove('open');
}

goView('lexikon');
</script>

</body>
</html>
